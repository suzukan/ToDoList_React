import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import './components.css';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


//NOTES
//<App/> is component
//this page is renderring App
//1. what to render (APP), 2. where to render (root)